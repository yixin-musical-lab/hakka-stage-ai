# AGENTS.md

## 协作规范

1. 编写代码时要加上清晰详细的注释，注释优先使用中文，方便课程组成员接手。
2. 本机配置为 RTX 3060 6G、R7 5800H、32G 内存。遇到长视频批量分析、大模型本地训练或微调、大规模视频生成、批量人声分离、多路视频并发推理等明显超过本机能力的任务时，不要在本机跑全量测试，应提醒放到云端 GPU 或服务器执行。
3. 第一阶段先保证 Docker 服务、前后端连通和工程结构清晰，不提前实现教案生成、视频分析、AI 报告等业务逻辑。
4. 新增接口时优先保证 OpenAPI 文档可读，接口命名保持中文业务语义和英文路径一致。


**原则**

1. **不要随手改无关代码**
   一个需求只改相关文件。不要顺手格式化整个项目、重命名一堆东西、重构无关模块，否则别人很难 review。

2. **先看现有结构，再写代码**
   Python 项目常见分层可能是：`router/controller`、`service`、`schema`、`model`、`config`、`utils`。
   不要按 Spring Boot 的习惯硬套，优先模仿项目已有写法。

3. **接口优先保持清晰**
   这个项目 AGENTS 里明确说了：新增接口要保证 OpenAPI 文档可读，接口命名保持“中文业务语义”和“英文路径”一致。
   也就是说，接口描述、summary、字段名、返回结构要让课程组和前端一眼看懂。

4. **第一阶段别提前做 AI 业务逻辑**
   当前项目约定是先保证 Docker 服务、前后端连通、工程结构清晰。
   不要急着实现教案生成、视频分析、AI 报告这些复杂业务，先把“骨架”搭稳。

5. **中文注释要写清楚**
   这个项目明确要求注释优先中文。尤其是接口用途、复杂逻辑、配置项、临时占位实现，要写给后续接手的人看。

6. **依赖不要乱加**
   Python 项目依赖管理比 Maven 更容易变乱。新增库前先问自己：项目里有没有已有方案？这个库是否必要？是否会影响 Docker 构建？是否需要更新锁文件或依赖文件？

7. **配置走环境变量，不要硬编码**
   数据库地址、API Key、模型路径、服务端口都不要写死在代码里。通常放 `.env`、Docker Compose、配置模块里。

8. **不要提交本地垃圾文件**
   常见不要提交的东西：`.env`、`__pycache__`、`.venv`、日志、临时视频、大模型权重、测试输出文件。

9. **本机别硬跑重任务**
   机器是 RTX 3060 6G，适合轻量验证，但不适合长视频批量分析、大模型训练/微调、大规模视频生成等任务。遇到这类需求，要建议放云端 GPU 或服务器。

10. **每次提交前自己跑一遍最小验证**
   至少确认服务能启动、接口能访问、Docker 或本地环境没被你改坏。团队协作里，“我这边没测”会让别人背锅。

**团队协作 Git 习惯**

- 从最新主分支拉代码再开自己的分支。
- 一个分支做一个清晰任务。
- commit 信息写清楚，比如 `feat: 添加课程列表接口骨架`。
- 不要直接改主分支。
- 不要强推公共分支，除非团队明确允许。
- 冲突时先理解别人改了什么，不要为了通过冲突直接删别人代码。

拿到任务后按这个顺序来：

1. 看 README、Docker Compose、后端入口、已有接口。
2. 找相似功能，模仿它的目录、命名、返回格式。
3. 小范围实现。
4. 写中文注释和 OpenAPI 描述。
5. 本地启动验证。
6. 提交前看一遍 diff，确认没有无关改动。
7. 发 PR 时说明：做了什么、怎么测的、还有什么没做。

一句话总结：
你现在最重要的不是“写很多代码”，而是写出别人能看懂、能运行、能 review、不会把工程节奏带偏的代码。这个项目第一阶段尤其要稳，先把 Docker、前后端连通、接口骨架和工程结构打牢。
